# manahej-backend

API backend for the educational curricula platform.

## Endpoints
- `GET /api/countries`
- `GET /api/grades`
- `GET /api/countries/:countryCode/grades/:gradeNumber/subjects`

## Local run
